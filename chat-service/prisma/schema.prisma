datasource db {
    provider = "sqlite"
    url      = "file:./chat.db"
}

model ChatMessage {
    id        Int      @id @default(autoincrement())
    userId    Int
    content   String
    createdAt DateTime @default(now())
}
